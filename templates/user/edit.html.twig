<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    </head>
{% block body %}
<body>
<style>
    body {
      background: #d7daed;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
      .custom-input {
          width: 100%;
          padding: 10px;
          margin-bottom: 15px;
          border: 1px solid #ccc;
          border-radius: 5px;
          font-size: 16px;
          box-sizing: border-box; /* Ensure padding and border are included in the width */
          text-align: left; /* Ensure text is aligned to the left */
        }
        
        .custom-button {
          width: 100%;
          padding: 12px;
          background-color: #ff656c;
          color: #fff;
          border: none;
          border-radius: 5px;
          font-size: 18px;
          cursor: pointer;
          text-align: center; /* Center-align the button text */
        }
        
        .edit-form {
          max-width: 400px;
          margin: 0 auto;
          padding: 20px;
          background: #fff;
          border: 1px solid #ccc;
          border-radius: 10px;
        }     
  </style>
<div class="edit-form">
    <h1>Profil</h1>
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.email,{'attr': {'class': 'custom-input'}}) }}
    {{ form_row(form.firstName,{'attr': {'class': 'custom-input'}}) }}
    {{ form_row(form.lastName,{'attr': {'class': 'custom-input'}}) }}
    {{ form_row(form.birth,{'attr': {'class': 'custom-input'}}) }}
    {{ form_row(form.gender,{'attr': {'class': 'custom-input'}}) }}
    {{ form_row(form.phone,{'attr': {'class': 'custom-input'}}) }}
    {{ form_row(form.city,{'attr': {'class': 'custom-input'}}) }}
    {{ form_row(form.companyId,{'attr': {'class': 'custom-input'}}) }}
    {{ form_row(form.password,{'attr': {'class': 'custom-input'}}) }}
    <button class="custom-button">{{ button_label|default('Sauvegarder') }}</button>
    {{ form_end(form) }}
</div>
</body>
{% endblock %}

</html>